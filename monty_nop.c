#include "monty.h"
/**
  *f_nop- Nothing will be displayed unfortunately
  *@head: first in line of queue
  *@counter: counts the number of lines to be displayed
  *Return: Nothing will be returned Always success
 */
void f_nop(stack_t **head, unsigned int counter)
{
	(void) counter;
	(void) head;
}

