#include "monty.h"
/**
 * f_stack – displays the top of the stack
 * @head: Head stack first in line
 * @counter: number of lines to be counted you check
*/
void f_stack(stack_t **head, unsigned int counter)
{
	(void)head;
	(void)counter;
	bus_file.pattern = 0;
}
