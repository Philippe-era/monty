#include "monty.h"
/**
 * f_stack - displays the top of the queue
 * @head: Head stack you know its the first in line
 * @counter: counts the number of lines in the queue
*/
void f_stack(stack_t **head, unsigned int counter)
{
	(void)head;
	(void)counter;
	bus.lifi = 0;
}
